# Docker configuration backend
providers:
    docker:
        defaultRule: "Host(`{{ trimPrefix `/` .Name }}.docker`)"
        exposedbydefault: false

# API and dashboard configuration
api:
   insecure: true
